# CLAUDE.md — IoMT Anomaly Detection (M.Sc. thesis)

Four-layer anomaly detection on CICIoMT2024: XGBoost (supervised) + autoencoder/VAE +
Isolation Forest, fused by an entropy gate; per-class TreeSHAP explainability. Python 3.13,
no GPU. Explainability is **SHAP only — LIME is NOT implemented** (DN-01).

Stack (verified installed): Python 3.13.13 · xgboost 3.2.0 · tensorflow 2.21 / keras 3.14 ·
scikit-learn 1.8 · shap 0.51 · pandas 2.3 · numpy 2.2. `requirements.txt` pins
`xgboost>=2.0,<3.0` but 3.2.0 is installed — a known drift; do not "correct" numbers to it.

This root is THIN. In a subsystem? Read its CLAUDE.md:
- `deliverables/scripts/CLAUDE.md` — frozen production pipeline (`run_all` + tripwires)
- `thesis/crossdataset/CLAUDE.md` — cross-dataset generalization (the four TRAPS live here)
- `notebooks/CLAUDE.md` — the `.py` training/eval modules (NOT ipynb)
- `dashboard/CLAUDE.md` — Streamlit app
Reference (link, don't inline): `README.md` (§-indexed compendium) · `deliverables/numbers_map.md`
(number→source) · `decisions/` (why-we-chose ledgers — **local, gitignored; absent in a clone**) ·
`docs/` (per-phase). Agent cycle:
`docs/agents-roster.md`. Rediscovery-killers: `docs/FINDINGS.md`.

## 1. DO NOT (hard prohibitions)
- **DN-01** — Do NOT claim a capability the code lacks. LIME is not in this repo (SHAP/TreeSHAP
  only); audit the code before any method/contribution claim. [burned: 7d86720]
- **DN-02** — Do NOT feed the tree-model RobustScaler ColumnTransformer to the AE/IF/VAE.
  Distance/reconstruction models need a dedicated StandardScaler fit on **benign-train**; the
  wrong scaler once caused a 510× AE-loss error. [92e59d4]
- **DN-03** — Do NOT fit any scaler / SMOTE / threshold on val, test, or the pre-split pool.
  Train-only (benign-train for the AE). [TRAP 1]
- **DN-04** — Do NOT edit a reported metric, citation, or figure without tracing it to its
  canonical source (`deliverables/numbers_map.md` / source paper / results artifact).
  numbers_map wins. [283a838, 7d86720]
- **DN-05** — Do NOT present a single-seed result as robust; aggregate over the seed set (INV-04).
  [3f61e59]
- **DN-06** — Do NOT `git add -A` / `--all` / blanket-stage. The tree carries untracked live
  thesis work, large derived artifacts, and two committed venvs — stage by explicit path only.
  Enforced on this machine by a local hookify guard (`.claude/hookify.*.local.md`, gitignored —
  not inherited by a clone).

## 2. CRITICAL INVARIANTS
- **INV-01** — Feature & label order is canonical: source it from the canonical `config.json`
  feature list (`feature_names` / `feature_names_reduced`), match by name then reorder — never
  by position; compare two models in the identical label space. [TRAP 3; faa4d14; README §5]
- **INV-02** — Fusion is tripwired: seed=42 must reproduce `entropy_benign_p95` strict_avg =
  `0.8035264623662012 ± 1e-9` (`_common.py:35`; §15D p93 anchor `0.8589586873140701` too).
  A drift is a real regression — investigate, don't loosen `TRIPWIRE_TOLERANCE`.
- **INV-03** — CICIoT2023 CSV ingestion strips trailing CRLF on the Label column (TRAP 2,
  `labels.py:41`). Silent label-match failure otherwise.
- **INV-04** — Seed sets: main pipeline `[1, 7, 42, 100, 1729]`; crossdataset `(42, 43, 44)`.
  `random_state=42` for reproducibility, the full set for stability.
- **INV-05** — Dedup caveat: headline numbers sit ~0.5–1.4 pp below published values (deduped
  test set). Every literature-comparison cell must name its caveat in the same row.

## 3. WORKFLOW
- **Before code:** read the subsystem CLAUDE.md + relevant `docs/phase*.md`. (`decisions/` holds
  local-only rationale ledgers — gitignored, absent in a clone; consult if present.) Don't
  re-derive a settled decision.
- **Writing code:** no test suite (below) — the safety net is the tripwire harness + verifiers;
  preserve them.
- **After code — gates (run verbatim):**
  ```
  venv/bin/python -m deliverables.scripts.run_all        # fail-fast, bit-exact tripwires
  venv/bin/python scripts/verify_report_numbers.py
  venv/bin/python scripts/verify_duplicate_counts.py
  ```
- **After any doc/status/number:** run `/fact-check` before committing (write→verify).
- **New feature:** `/plan-feature` → house-format plan in `docs/plans/`.
- **Fixed a rule-rooted bug?** add/upgrade a DN-/INV- rule here.

## 4. Skill config
<!-- machine-read by skills (/brief, /senior-review, /red-team, /fact-check). Precedence: explicit args > this section > skill defaults. -->
- oracles: deliverables/numbers_map.md (every reported number — DN-04), results/ artifacts, tripwire anchors in deliverables/scripts/_common.py:35; README.md §-index for prose claims
- stack: Python 3.13.13 · xgboost 3.2.0 (manifest pins <3.0 — known drift, do not "correct") · tensorflow 2.21 · scikit-learn 1.8 · shap 0.51 · seeds [1,7,42,100,1729] / crossdataset (42,43,44)
- default-flags: senior-review=--paper, red-team=evaluator,ops
- languages: docs=English, figures=_en+_tr pairs, stakeholder=Turkish

## 5. CURRENT STATUS
### Systems built
| Layer | What | Where |
|---|---|---|
| L1 supervised | XGBoost/RF, E7 macro-F1 = 0.9076 | notebooks/supervised_training.py, results/supervised/ |
| L2 unsupervised | AE + Isolation Forest, AE test AUC = 0.9892 | notebooks/lstm_ae_train.py, results/unsupervised_unscaled/ |
| Fusion | entropy-gate 5-case | notebooks/fusion_engine.py, results/fusion/ |
| Explainability | per-class TreeSHAP | notebooks/shap_analysis.py, results/shap/ |
| Generalization | CICIoMT2024→CICIoT2023 | thesis/crossdataset/, results/tausweep/ |
| Dashboard | Streamlit (local) | dashboard/ |

(Numbers above trace to `deliverables/numbers_map.md` rows 16 & 21.)

### Tests
No pytest / lint / CI in this repo. The gate is `run_all`'s bit-exact tripwires +
`scripts/verify_*.py` number audits. Say "tripwires reproduce", never "tests pass".

### Recently closed (newest first)
- **Thesis figure set: 7 Turkish figures, parity-checked against the existing English set (2026-07-30/31 · IoMT 9118540, ee6f848 · manuscript 98cb076→f8f2081, EXCLUDING ef75d5a which interleaves — that is the P1 session's unpark commit)** — the first figure pass (5 figures, logged in the entry below) was **challenged by the user: "didn't we already have them?"** — and the challenge was correct. Directory listings had been inventoried, figure *contents* had not: **Şekil 3.2 duplicated `fig01_class_distribution.png` and Şekil 4.3 duplicated `ablation_comparison.png`, both losing information in the rebuild** (3.2 dropped the test bars; 4.3 dropped H2-binary and flag-rate). Verified by grep that the other three had no counterpart anywhere (zero dedup/tau/transfer figures exist). Fix: both rebuilt to **information parity** with their English originals, then extended — 3.2 regained train+test after dedup with the 1.635.956/689 endpoints labelled; 4.3 regained the two series and gained a fourth (**benign FPR**, so the rescue gain and its 0,189→0,229 cost sit in one chart). Two more figures then added — **and the arc's own fact-check caught the same error repeating**: **Şekil 4.1 E7 karışıklık matrisi** and **Şekil 4.2 AE/IF ROC** were described as "genuinely missing" but both already existed in English (`docs/figures/fig05_e7_confusion_matrix.png` carries the identical 0,33 / 0,55→Benign / 0,57 / 0,81 values; `fig11_ae_vs_if_roc.png` prints the same AUCs) — in the very directory the new process rule names. The **Şekil 3.1** counterpart also exists, in `paper/P1_dedup_audit/figures/fig2_per_class_structure_en.png`, committed by the sibling P1 session the day before and *richer* (adds a cumulative-mass panel). **Honest final tally: 5 of the 7 Turkish figures (3.1, 3.2, 4.1, 4.2, 4.3) are translations of existing English figures; only 4.6 and 4.7 are genuinely new** (`fig13_threshold_sweep.png` is the fusion percentile sweep, a different quantity). That is a fine outcome for a Turkish thesis — the narrative was what was wrong, not the figures. Recomputation still earned its keep: the ROC AUCs re-derive to 0,9892 / 0,8612 and the confusion matrix's clean diagonal confirms the classification-report label order matches the matrix axes. All seven placed in Bölüm 3/4 with captions verified against artifacts (one known imprecision: Şekil 3.1's caption names only Benign and MQTT as zero-duplicate; `TCP_IP-DDoS-UDP` is also exactly zero — fix on the read-through pass). **Process rule (widened after it failed once): before generating any Turkish figure, `find . -name '*.png'` across `docs/figures/` (39), `results/*/figures/` (63, incl. nested subdirs — the shallow glob undercounts by 3), `eda_output/figures/` (21) AND `paper/**/figures/` (5) — 128 tracked English PNGs; the narrow two-directory version of this rule would still have missed the Şekil 3.1 counterpart.** The ~120 not translated are diagnostics left in English by choice. Everything is scripted (`deliverables/scripts/08_thesis_figures_tr.py`), so a number change regenerates in one command. **Cross-session note:** the parallel P1 session finished its manuscript (11 sections, ~8,700 words, 7 tables, 5 figures, 25 refs), built the `ciciomt2024-dedup-audit` repro package (91 checks passing), applied a manuscript fact-check (IoMT ef8333b, 10 WRONG), and **unparked the hoca batch** (manuscript ef75d5a) — now SEND-READY with six checkbox questions covering thesis feedback, defense date, P1 submission, authorship/order, APC, and repro-package publication, plus a plainly-stated premium withdrawal. Consistent with A2 (one batch, after the full draft — which now exists).
- **Thesis gates closed: §4.5 + 0/18 reconciliation + 5 TR figures (2026-07-30 · IoMT 98381b4→6c20ca6 · manuscript 5dc2f13→97fc700 · lit-review 936114d, decfe16, 01297b1)** — the three blockers on the A2 hoca batch, all closed. **(1) §4.5 filled (A3 satisfied).** User's tau-sweep/generalization work committed by explicit path (small artifacts only; `thesis/crossdataset/artifacts` 526M and `sampled_test_set.parquet` 21M stay untracked) → **`numbers_map` Section 13** (11 generalization oracle rows, provenance corrected mid-arc to name `iomt-pcap-experiments/track_a_out/*` after a verifier caught that ~40% of §4.5 traced to a *different repo* than the row cited) → §4.5 written as a condensed TR rendering: cross-dataset transfer at chance (bal-acc 0,4998), **AE inversion 0,2586** (below random — "anomalous" doesn't travel), device-disjoint **infeasible** (5 victim devices, no class ≥3 — a finding, not a gap), session-disjoint gap **0,259 ± 0,047** (5/5 seeds; quantifies the merge-and-re-split leakage at ≈+0,26 macro-F1), tau operating characteristic (81,1% of errors in the 1,676% deferred slice). Verifier: **9 wrongs fixed**, two blocking — an over-generalized "MQTT is session-invariant" claim its own artifact refutes (**MQTT_DoS_Publish_Flood gap 0,51**, now named with its 381-flow small-support caveat) and the wholesale loss of the draft's threats-to-validity block (restored). It also correctly *declined* to restore the English draft's own unsupported "69–90% deferred" range. Ch5 items 3+8 de-staled (generalization now measured, negative, owned). **(2) 18-vs-19 RESOLVED to 0/18** — the artifact decides: `multi_seed_per_target_summary.csv` per-target n_seeds = 5+0+5+3+5 = **18**, vindicating `full_report`'s 25−5−2 arithmetic; `numbers_map`'s 0/19 and the notebook printout under-counted Recon_Ping_Sweep's exclusions (2, not 1). Swept across **17 files in 3 repos** (15 carry a literal 19→18 edit; the other two are the AUDIT resolution note and this queue); **AUDIT M6 closed**; `[TUTARLILIK NOTU]` cleared from manuscript §4.3.3/§5.4. **(3) Memorization-premium sweep done jointly with the P1 session** (gap report + summary via lit-review 936114d; Riyadi attribution decfe16; thesis Ch1 §1.1 via manuscript **5dc2f13**) — the C1 five-seed ablation retires the 0.54pp premium; what survives is the separable **test-set effect +0,35–0,40pp**; v6.6 verified as cost-framing, unchanged. **(4) Five Turkish figures** (`08_thesis_figures_tr.py` → `thesis/manuscript/figures/`; **superseded — the set is now 7 and two of these five were rebuilt for parity, see the figure-set entry above**): Şekil 3.1 per-class dedup (train rate 86,32% — switched from the unanchored pooled 87,8%), 3.2 imbalance, 4.3 ablation, **4.6 tau accuracy–coverage** and **4.7 cross-dataset transfer** (the two the generalization draft asked for and nobody had drawn); placed in Bölüm 3/4 with captions fact-checked against artifacts. Rendering-and-looking caught three defects code review would not: **Turkish glyphs silently absent** (ASCII "Sekil/dogruluk"), an annotation over the legend, and family labels falling back to raw English keys after a string edit broke the mapping; Turkish decimal ticks added. Gates re-run in-session (generated `verification_report.md` again left unmodified, so it corroborates the counts rather than timestamping the run): `verify_duplicate_counts` clean; `verify_report_numbers` 65 GREEN / 23 YELLOW / 5 RED (the same 5 pre-existing course-project REDs). English result figures deliberately left unconverted — see the corrected inventory (128 tracked) in the figure-set entry above.
- **Thesis full draft (5/5 chapters, TR) + paper program frozen + manuscript repo (2026-07-30 · iomt-thesis-tr 08d6725→3458a02 · IoMT ce7d641, 5612ae2)** — Paper portfolio decided (P1 dedup-measurement / P2 zero-day+EVT / P3 framework; P4 survey stays Chapter 2) and **four briefs frozen** in `.claude/plans/` (p1-dedup, p2-zeroday, p3-framework, thesis-writing; local-only). Thesis brief amendments: **A1** (§1.3 Tezin Yapısı addition), **A2** (chapter gate REVERSED to full-draft-then-review — ONE hoca batch at the end; batch template `thesis/manuscript/hoca_batch_parked.md` — **unparked and made send-ready 2026-07-31**), **A3** (§4.5 stubbed until tau-sweep fold-in). New **private** nested repo `thesis/manuscript/` → github.com/amirbaseet/iomt-thesis-tr (lit_review pattern; parent gitignored at ce7d641; NEVER public — Ch2 derives from lit_review). **All 5 Turkish chapters drafted (~24,200 words)** — Ch1 hand-drafted; Ch2–5 via **31 subagents in 4 Workflow runs** (translate/compose → per-section adversarial verify → terminology harmonize), every chapter through the internal gate (draft → oracle-trace → fact-check → fix → commit): **Ch2** = v6.6 translation, 7 chunks, 1,207 numbers verified, **0 factual errors**, 134 harmonizer edits, refs verbatim, "vd." convention (65 conversions); **Ch3** = 10 verifier fixes (killed rarity overclaim resurfaced and re-killed; canonical 4-axis alignment; 5-not-6 attack categories); **Ch4** = 11 wrongs + 8 honesty gaps fixed (CatBoost attribution, IF-scope, 0/5-vs-0/4 denominator text, seed-42 disclosures, ensemble/full_enhanced negatives discussed), tables 4.1–4.8, §4.5 = A3 stub; **Ch5** = 6 fixes (Riyadi/Yacoubi anchor cross-ref, +deployment-cost-budget future-work item). `terminoloji.md` grown to 32+ fixed terms. Oracle extended: AE p95/p99 val rows (5612ae2, from `thresholds.json:evaluation_on_val`). **Surfaced at this arc's close, both RESOLVED 2026-07-30 (see the gates entry):** (1) the 18-vs-19 eligible-cell accounting (artifact says 18; swept to 0/18, inside that entry's ranges); (2) the memorization-premium retirement following the P1 session's C1 sweep (81581c4) — corrected by lit-review 936114d/decfe16 and manuscript 5dc2f13, work performed jointly with the P1 session rather than inside this repo's gate commits. Parallel P1-session IoMT commits (831b7f0 through 9160d63, EXCLUDING this arc's ce7d641 which interleaves in that range — P1 outline/C1 ablation/manuscript) are NOT covered by this entry; this arc's IoMT commits are ce7d641 and 5612ae2 only.
- **Advisor 1C/2A + exec summary + σ-oracle fix + §2.6 refs + DR-6b re-anchor (2026-07-30 · IoMT bbe7359→627c2c4 · lit-review c987d03→07fa640 · pcap 6fc507d→27b5ad7)** — advisor confirmed the gap-report format **1C/2A** (full English report + 2–3-page summary, both): `Research_Gap_Summary_v1.0.md` written and adversarially fact-checked (55 claims, 49 confirmed; **4 directional omissions restored** — the Moderate defensibility grades, the "in some form" dedup hedge, the fifth-study variance note, the limitations block — every cut hedge was a claim-weakening one, the classic condensation failure); report footer flipped to format-confirmed; both docs also published as private artifacts. The fact-check surfaced a **genuine oracle defect**: `numbers_map.md` row 250 said σ=0.022 while the artifact (`multi_seed_summary.csv` h2_strict_avg_std=0.02254) and README §15B.3 say **0.023** — commit 7d86720 had fixed 7 spots (README ×3 + Project_Journey ×4) but missed the oracle row, which then seeded 0.022 downstream. Fixed oracle-first per DN-04, then swept the remaining **14 occurrences across 9 files in both repos** (15/10 counting the oracle row). **v6.6 §2.6 reference debt closed**: refs 30–33 added (bibliographic fields verbatim from the CrossRef-verified summaries), related-work renumbered 34–42 and the foundational tail 43–47 (first pass missed the tail → transient duplicate 42, caught by a sequence validator; final list 1–47 clean; no in-text numeric citations exist, so prose needed no renumbering). **DR-6b closed the ~86% re-anchor debt** (`iomt-pcap-experiments/dr6b_perclass_f32.py` + `dr6_out/dr6b_perclass_f32.json`, dr6-identical hashing conventions; renamed from DR-7 mid-arc to avoid colliding with the gap doc's defense item DR-7): per-class float32 within-class rates reproduce PJ's **DDoS-ICMP 86.32% (train) exactly**; test **94.37%**; TCP_IP-* floods carry **99.5%** of train within-class duplicate mass; three rows anchored in `numbers_map.md` §2 and v6.6's re-anchor-before-submission marker cleared. The recomputation also falsified v6.6's "negligible non-flood duplication" wording — **Recon sub-types are 6–16% by rate (only ~0.5% of mass); zero holds only for Benign/MQTT** — reworded honestly. Gates re-run in-session at HEAD (generated `verification_report.md` deliberately left unmodified): `verify_report_numbers` 65 GREEN / 23 YELLOW / 5 RED — the 5 RED pre-date the arc (same counts on the pre-edit stash) and sit in the course-project doc the script audits, untouched here; `verify_duplicate_counts` clean. No pipeline code touched — the arc's only new code is pcap's standalone DR-6b analysis script. **PJ-archive blocker removed** (the ~86% figure no longer traces only to `Project_Journey_Complete.md`). The report+summary send was open at this arc's close — **superseded by A2 (2026-07-30, see thesis entry): everything goes to hoca in ONE batch after the full thesis draft.**
- **#31–#34 integration + standalone gap report v1.0 (lit-review ed3c43d→9c7ab57)** — 3-BLUE-agent propose pass (37 blocks, indexed in `INTEGRATION-PROPOSALS-FINDINGS.md`) → **G2 decision: Strong-on-conjunction, harmonized in both docs** → 49 blocks applied across the gap doc (G2/G3 rewritten, six-column §3.3 ladder, new §5.7 PSM-HT head-to-head, §0 fact-5 frozen-vs-expanded frame, rosters G8=5/G10=4, G13 parity row) and v6.6 (Table 2.1 rows 30–33, **Table 2.2 R10 removed as a duplicate of row 32/Mahbub**, membership 33=31+2 (the v6.6/report roster: includes #9 Gheni, excludes Özdoğan — reconciles with the prior entry's 34-entrant/32-full-text summary-file tier via the sweep file's numbering map), §2.2.5 three-implement-and-evaluate + Jodayree subsection, §2.4 rewordings). Cross-doc verify: G2 harmony + zero numbering contamination confirmed; 6 fixes applied (XAI count is **22/33** — machine-counted; dangling G13; ViT row; ordinal; numbering-map guard added to the sweep file). Then **`Research_Gap_Report_v1.0.md` drafted per the frozen brief** (exec summary / landscape / G1–G13 table / zero-day frontier / positioning / 33-row corpus appendix, ~4,000 words); senior review (--paper): **GO-WITH-FIXES, factual core fully verified, overclaim audit passed**; 3 CRITICALs (Appendix-B quarantine arithmetic, provenance-line 31+2, missing Saeed in the roster) + 12 recommendations all applied. Done-check satisfied: review ran, no CRITICAL survives, committed. Report format was pending advisor confirmation at this arc's close — **resolved 2026-07-30 (1C/2A — see the Advisor 1C/2A entry)**. Known debt at close: v6.6 §2.6 reference entries for rows 30–33 + related-work renumbering — **closed by the Advisor 1C/2A entry**.
- **Corpus expansion #31–#34 + 2026-07 sweep (lit-review 7160a8b→8f0e71d)** — brief frozen (`.claude/plans/2026-07-29-litgap-report-brief.md`, local-only: `.claude/` gitignored) for the advisor-requested standalone English gap report; 102-agent deep-research sweep (19 confirmed/6 refuted claims → `SWEEP-2026-07-FINDINGS.md`; two refutations later **overturned by primary-source fetches** — lesson recorded: 0-3 votes on redirect-gated sources measure fetchability, not truth). Four entrants summarized and adversarially verified: **#31 Jodayree PSM-HT** (HIGH-risk zero-day rival: 18-type LOO all-succeeding, energy+EVT, session-disjoint splits, no dedup, ~167:1 window-flattened imbalance), **#32 Palaniappan MI+DL-BDA** (Wilcoxon+Cohen's-d → G10's exceptions become four; Recall@FPR≤1%; 4th resolvable code link), **#33 Mahbub CWD-DA** (71-instance Ping Sweep class — sharpest G9 exhibit; Tables 5≡7 identity; note: commit 8003333 briefly pushed unfixed files under a fixing message — corrected honestly in fca4684), **#34 Al-Hasani FSLM+KDN** (abstract-tier like #09; its abstract's "1.5M/42/15/real-hospital" dataset description conflicts with canon on every element). ALL_SUMMARIES rebuilt ×4 (33 files, #01–#34, gap at #09; full-text tier = 32 — only #34 is abstract-tier among the files; #09 has no file). ⚠ Gap doc + v6.6 were left STALE vs the expanded corpus at this arc's close — **resolved by the next arc (integration applied 2026-07-29, see entry above)**.
- **Chapter-2 prose v6.6 (lit-review 1cd14db→ea57fc1)** — drift-checked `Literature_Review_Chapter2_v6.5.md` against the corrected gap doc (41 findings, 10 CRITICAL — spec in `thesis/lit_review/V6.5-DRIFT-FINDINGS.md`), then rewrote as **v6.6**: §2.4.2(a) rebuilt on the DR-6 float64-train-split↔float32 resolution (the disproved "pre-redistributed dataset / 500×" theory removed); G2/G5/G7 premises corrected to survive published precedent; dedup roster 6→9; leakage frame 2→4 axes; Hafid operational head-to-head added; gap table extended to G13 (new G8–G12 + former profiling gap renumbered G13); 8 `[Phase-log:]` tags citing 4 phase docs. Adversarial verify (report in-session, not persisted): **39/41 spec findings fixed, 10/10 §2.4.2(a) reconciliation numbers exact vs `dr6_out/`, 0 absence-credit regressions** → follow-up fixes applied, incl. 3 pre-existing v6.5 reference errors (row/ref 18 authorship is **Akkal** et al., ICSPIS 2024 — load-bearing for the DR-6 provenance argument; #14 is ICISSP 2025, not SECRYPT; #27 IncFL is an arXiv preprint, not a paged conference paper). Editorial decisions closed: corpus-membership rule declared in §2.2 (29 = 28 full-text + #9 abstract-only; absence claims over the 28) and six-works head-to-head framing (Lipsa routed to §2.4.3). One flagged item inside v6.6: the ~86% DDoS-ICMP within-class duplication figure was marked for re-anchoring to `numbers_map.md` before submission — **re-anchored by DR-6b, 2026-07-30 (see the Advisor 1C/2A entry)**.
- **Lit-review senior review + DR-6 resolution (320b5d7 · lit-review 55dfc58 imported-with-fixes-applied + b41e026/33efead DR-6 · pcap aaf8917)** — 3-agent adversarial review of `thesis/lit_review/Chapter2_Synthesis_and_Gap_Analysis.md` (verdict NO-GO→GO-WITH-FIXES: G2/G7 false as worded vs Alfageer #21/Akar #08; 27→29-paper frame; dedup roster 5/27→9/29) + **43 of 46** BLUE proposals applied (3 superseded duplicates skipped; the rewrite predates the repo's first commit, so no reviewable diff — `FINDINGS.md`/`PROPOSALS-FINDINGS.md` are the provenance): new gaps G8–G12, DR-9–DR-12, Büken/Hafid head-to-heads, core internal numbers carry resolvable `[Phase-log:]` citations (cite-once-inherit convention, §0 fact 4; one cites `pathB_hardening.md`). **DR-6 closed by recomputation** (`iomt-pcap-experiments/dr6_*.py`): 5,119 = float64-exact duplicates of the released train split (reproduced exactly; Akkal's subset truly has 672 — his 5,119 is pipeline-inherited); thesis 36.95%/44.72% reproduce at **float32** to 4 decimals (2,645,751/721,914). Wording "bit-identical at float32" applied in `docs/phase2_eda.md`; cross-check: `scripts/verify_duplicate_counts.py`'s features-only pooled count (3,368,126) reproduces DR-6's float32 full-merge figure. lit_review now versioned in **private** repo `iomt-lit-review` (canonical; parent repo gitignores the dir; Downloads copy secondary).
- **Literature-figure restore (283a838)** — re-verified COCIA/Recon_VulScan/AIAI vs numbers_map.
- **LIME cleanup + sigma fix (7d86720)** — removed LIME capability claims; sigma 0.022→0.023 ×7.
- **Path B Tier-2 LSTM-AE (c3e3f34)** — Layer-2 substitution + 4-issue calibration audit.

### Workflow next (work this queue, don't re-derive)
0. ~~Gap-doc + v6.6 integration~~ **DONE 2026-07-29**; ~~advisor format confirmation + §2.6 refs~~ **DONE 2026-07-30**; ~~thesis full draft (5 chapters)~~ **DONE 2026-07-30** (see the thesis full-draft entry). Hoca delivery is ONE batch per A2 — **all three gates cleared and the batch is SEND-READY** (`thesis/manuscript/hoca_batch_parked.md`, unparked 2026-07-31 by the P1 session: six checkbox questions covering thesis feedback, defense date, P1 submission, authorship/order, APC, repro-package publication). Remaining before sending: the user's read-through and the institute format pass.
1. ~~Fold tau-sweep into the generalization section / fill manuscript §4.5~~ **DONE 2026-07-30** (artifacts committed 98381b4; §4.5 written + verified; A3 stub closed).
1b. ~~Reconcile the 18-vs-19 eligible-cell accounting~~ **RESOLVED 2026-07-30: artifact says 18** (`multi_seed_per_target_summary.csv` n_seeds 5+0+5+3+5); numbers_map/pathB/full_report/decisions_ledger/AUDIT/lit-review/manuscript all corrected to **0/18**; TUTARLILIK NOTU cleared. Residual: README (L1272/1306/1347/1500) + PJ (L453 has a third wrong accounting "19=20−1"; L946 also says 0/19) still say 19 — fix rides with the user's pending README/PJ commits (queue 4).
1c. ~~Memorization-premium doc sweep~~ **RESOLVED 2026-07-30**: gap report §3 + summary corrected by the P1 arc (lit-review 936114d); thesis Ch1 §1.1 rewritten with the withdrawal + separable +0.35–0.40pp finding (manuscript **5dc2f13**); v6.6 §2.2.8 verified as cost-framing (no premium claim — survives unchanged).
2. Decide xgboost pin: bump manifest to allow 3.2.0, or pin the env down.
3. Cleanup: `venv/` and `venv_old/` are committed to the tree — decide whether to untrack.
4. README duplicate-wording sweep (surfaced by DR-6, README has uncommitted edits — do when committing it): "exact duplicates" needs the float32 qualifier at **L547, L844, L2522**; L547's "**not reported in any prior paper**" is the falsified rarity overclaim the review killed (9/29 touch dedup) — reword per the gap doc's §3.1 defensible statement; L2065's "shared pre-redistributed input dataset" inference is **superseded** — DR-6 shows 5,119 is the correct float64 count of the released train split (Riyadi/Kharoubi scopes = train dir; Akkal inherited). Optionally add the precision note to `numbers_map.md` rows 36–37 (DN-04: it's the oracle — same-value, qualifier only).

❓ Pending decisions: keep vs archive `Project_Journey_Complete.md` (**~86% archive blocker removed 2026-07-30** via DR-6b; one PJ-only oracle row remains — `numbers_map.md` "LOO Phase 6B runtime 19.3 min" cites PJ Phase 6B — re-source or accept before archiving; PJ also has uncommitted user edits, so still the user's call); whether the venvs stay tracked. (`chore/bootstrap-claude` in sync with origin as of 2026-07-30.)
